/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { UserMessage } from '../../models/user-message';

export interface ApiGeminiChatChatIdPost$Plain$Params {
  chatId: string;
      body?: UserMessage
}

export function apiGeminiChatChatIdPost$Plain(http: HttpClient, rootUrl: string, params: ApiGeminiChatChatIdPost$Plain$Params, context?: HttpContext): Observable<StrictHttpResponse<any>> {
  const rb = new RequestBuilder(rootUrl, apiGeminiChatChatIdPost$Plain.PATH, 'post');
  if (params) {
    rb.path('chatId', params.chatId, {});
    rb.body(params.body, 'application/*+json');
  }

  return http.request(
    rb.build({ responseType: 'text', accept: 'text/plain', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<any>;
    })
  );
}

apiGeminiChatChatIdPost$Plain.PATH = '/api/GeminiChat/{chatId}';
